version: '3'

services:
  genomesight-front:
    container_name: genomesight_frontend
    image: genomesight_frontend
    restart: always

    networks:
      - genomesight-networks


  genomesight-back:
    container_name: genomesight_backend
    build:
      dockerfile: ./Dockerfile
    networks:
      - genomesight-network
    restart: always

  nginx:
    container_name: genomesight_nginx
    restart: unless-stopped
    image: nginx
    ports:
      - 6628:80
    volumes:
      - ./nginx:/etc/nginx

    networks:
      - genomesight-network

networks:
  genomesight-network:
    driver: bridge